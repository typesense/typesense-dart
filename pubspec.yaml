name: typesense
description: Dart client library for accessing the Typesense HTTP API.
version: 0.0.1
homepage: https://typesense.org/

environment:
  sdk: ">=2.10.0-0 <3.0.0"

dependencies:
  http: ^0.13.3
  crypto: ^3.0.1
  equatable: ^2.0.2

dev_dependencies:
  test: ^1.17.7
  mockito: ^5.0.10
