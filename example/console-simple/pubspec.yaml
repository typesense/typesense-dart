name: console_simple
description: A simple command-line application to showcase typesense examples.
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.6.1

dependencies:
  typesense:
    path: ../../
  logging: ^1.3.0

dev_dependencies:
  lints: ^5.1.1
